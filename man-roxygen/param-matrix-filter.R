#' @param lowerB Lower bound for filtering the input columns, the minimal
#'   column sum. Default = 0.0.
#' @param upperB Upper bound for filtering the input columns, the maximal
#'   column sum. Default = 1000.0.
